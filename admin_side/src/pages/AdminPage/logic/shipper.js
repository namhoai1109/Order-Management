

/*
const onDeleteShipper = (record) => {
        Modal.confirm({
            title: "Are you sure, you want to delete this Shipper record?",
            okText: "Yes",
            okType: "danger",
            onOk: () => {
                setDataSource((pre) => {
                    return pre.filter((Shipper) => Shipper.id !== record.id);
                });
            },
        });
    };
    const onEditShipper = (record) => {
        setIsEditing(true);
        setEditingShipper({ ...record });
    };
    const resetEditing = () => {
        setIsEditing(false);
        setEditingShipper(null);
    };
*/